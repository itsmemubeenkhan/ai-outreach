<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold text-slate-900">Outbound Email Log</h1><p class="mt-1 text-sm text-slate-500">Immutable delivery attempts without credentials or message bodies.</p></x-slot><div class="p-5 lg:p-8"><form class="mb-5 flex gap-3 rounded-xl border bg-white p-4"><select name="status" class="rounded-lg border-slate-200 text-sm"><option value="">All statuses</option>@foreach(['queued','processing','sent','failed','skipped','cancelled'] as $value)<option value="{{ $value }}" @selected(request('status')===$value)>{{ ucfirst($value) }}</option>@endforeach</select><button class="rounded-lg bg-slate-900 px-4 text-sm font-bold text-white">Filter</button></form><div class="overflow-x-auto rounded-2xl border bg-white"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="p-4">Recipient</th><th>Business</th><th>Campaign</th><th>Sender</th><th>Step</th><th>Status</th><th>Sent</th><th>Result</th></tr></thead><tbody class="divide-y">@forelse($emails as $email)<tr><td class="p-4 font-semibold">{{ $email->recipient_email }}</td><td>{{ $email->lead->business_name?:'—' }}</td><td>{{ $email->campaign->name }}</td><td>{{ $email->sendingAccount?->email?:'Not assigned' }}</td><td>{{ $email->sequenceStep->position }}</td><td><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{{ $email->status }}</span></td><td>{{ $email->sent_at?->format('M j, H:i')?:'—' }}</td><td class="max-w-xs truncate text-xs text-slate-500">{{ $email->failure_reason }}</td></tr>@empty<tr><td colspan="8" class="p-16 text-center text-slate-500">No outbound attempts yet.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $emails->links() }}</div></div></div></x-app-layout>
