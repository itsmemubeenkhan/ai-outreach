<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold text-slate-900">CSV Imports</h1><p class="mt-1 text-sm text-slate-500">Stream large prospect files through the queue.</p></x-slot><div class="p-5 lg:p-8"><form method="POST" action="{{ route('imports.preview') }}" enctype="multipart/form-data" class="mb-6 rounded-2xl border-2 border-dashed border-indigo-200 bg-indigo-50/50 p-8 text-center">@csrf<div class="text-3xl">⇧</div><h2 class="mt-2 font-bold text-slate-900">Upload a CSV to preview and map</h2><p class="mt-1 text-sm text-slate-500">Maximum 100 MB per upload. The importer never loads the full file into memory.</p><input required type="file" name="csv" accept=".csv,text/csv" class="mt-5 text-sm"><button class="ml-3 rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-bold text-white">Preview</button>@error('csv')<p class="mt-3 text-sm text-red-600">{{ $message }}</p>@enderror</form>
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-4">File</th><th>Status</th><th>Progress</th><th>Imported</th><th>Duplicates</th><th>Rejected</th></tr></thead><tbody class="divide-y">@forelse($imports as $import)<tr><td class="px-5 py-4 font-semibold">{{ $import->original_filename }}</td><td><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold">{{ $import->status }}</span></td><td>{{ number_format($import->processed_rows) }} / {{ number_format($import->total_rows ?: $import->processed_rows) }}</td><td>{{ number_format($import->imported_rows) }}</td><td>{{ number_format($import->duplicate_rows) }}</td><td>{{ number_format($import->rejected_rows) }}</td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-500">No imports yet.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $imports->links() }}</div></div></div></x-app-layout>
