<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold text-slate-900">Map CSV columns</h1><p class="mt-1 text-sm text-slate-500">Choose which lead field each source column represents.</p></x-slot><div class="p-5 lg:p-8"><form method="POST" action="{{ route('imports.store') }}">@csrf<div class="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr>@foreach($headers as $i=>$header)<th class="min-w-48 p-4 text-left"><div class="mb-2 font-bold text-slate-900">{{ $header ?: 'Column '.($i+1) }}</div><select name="mapping[{{ $i }}]" class="w-full rounded-lg border-slate-200 text-xs"><option value="">Skip column</option>@foreach($fields as $field)<option value="{{ $field }}" @selected(str($header)->lower()->replace([' ','-'],'_')->value()===$field)>{{ str($field)->replace('_',' ')->title() }}</option>@endforeach</select></th>@endforeach</tr></thead><tbody class="divide-y">@foreach($rows as $row)<tr>@foreach($headers as $i=>$header)<td class="max-w-64 truncate p-4 text-slate-600">{{ $row[$i] ?? '' }}</td>@endforeach</tr>@endforeach</tbody></table></div><div class="mt-5 flex justify-end gap-3"><a href="{{ route('imports.index') }}" class="rounded-xl border px-5 py-2.5 text-sm font-bold">Cancel</a><button class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-bold text-white">Queue import</button></div></form></div></x-app-layout>
