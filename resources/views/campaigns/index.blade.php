<x-app-layout><x-slot name="header"><div class="flex items-end justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Campaigns</h1><p class="mt-1 text-sm text-slate-500">Filter-defined audiences and multi-step outreach.</p></div><a href="{{ route('campaigns.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white">New campaign</a></div></x-slot><div class="p-5 lg:p-8"><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-4">Campaign</th><th>Brand</th><th>Status</th><th>Audience</th><th>Steps</th><th>Daily limit</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($campaigns as $campaign)<tr class="hover:bg-slate-50"><td class="px-5 py-4"><a href="{{ route('campaigns.show',$campaign) }}" class="font-bold text-slate-900 hover:text-indigo-600">{{ $campaign->name }}</a><div class="text-xs text-slate-500">{{ $campaign->category ?: 'All categories' }}</div></td><td>{{ $campaign->brand->name }}</td><td><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $campaign->status==='active'?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-700' }}">{{ $campaign->status }}</span></td><td><div class="font-semibold">{{ number_format($campaign->audience_count) }}</div><div class="text-xs text-slate-500">{{ $campaign->audience_status }}</div></td><td>{{ $campaign->sequence_steps_count }}</td><td>{{ number_format($campaign->daily_limit) }}</td><td><a class="font-bold text-indigo-600" href="{{ route('campaigns.edit',$campaign) }}">Edit</a></td></tr>@empty<tr><td colspan="7" class="p-16 text-center text-slate-500">No campaigns yet. Create one to define an audience and sequence.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $campaigns->links() }}</div></div></div></x-app-layout>
