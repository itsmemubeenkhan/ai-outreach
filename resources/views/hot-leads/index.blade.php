<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold">🔥 Hot Leads</h1><p class="text-sm text-slate-500">Buying signals requiring a human response.</p></x-slot><div class="grid gap-4 p-5 lg:grid-cols-2 lg:p-8">@forelse($messages as $m)<a href="{{ route('inbox.show',$m) }}" class="rounded-2xl border border-rose-100 bg-white p-6 shadow-sm"><div class="flex justify-between"><h2 class="font-black">{{ $m->lead?->business_name?:$m->from_email }}</h2><span class="rounded-full bg-rose-100 px-3 py-1 text-xs font-bold text-rose-700">{{ $m->classification }}</span></div><p class="mt-4 line-clamp-3 text-sm text-slate-600">{{ $m->body_text }}</p><div class="mt-4 border-t pt-4 text-sm"><strong>Next:</strong> {{ $m->recommended_next_action }}<span class="float-right font-black">Score {{ $m->lead?->lead_score }}</span></div></a>@empty<div class="col-span-2 rounded-2xl border border-dashed p-16 text-center text-slate-500">No hot leads need action.</div>@endforelse<div class="col-span-2">{{ $messages->links() }}</div></div></x-app-layout>
