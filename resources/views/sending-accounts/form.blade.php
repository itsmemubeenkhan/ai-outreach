<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold text-slate-900">{{ $sendingAccount->exists?'Edit':'Add' }} sending account</h1><p class="mt-1 text-sm text-slate-500">Credentials are encrypted using the application key and never displayed after saving.</p></x-slot><div class="p-5 lg:p-8"><form method="POST" action="{{ $sendingAccount->exists?route('sending-accounts.update',$sendingAccount):route('sending-accounts.store') }}" class="mx-auto max-w-5xl space-y-6">@csrf @if($sendingAccount->exists)@method('PUT')@endif @if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif
<section class="rounded-2xl border bg-white p-6"><h2 class="font-bold">Identity and limits</h2><div class="mt-4 grid gap-4 md:grid-cols-2">@foreach(['name'=>'Account name','sender_name'=>'Sender name','email'=>'From email'] as $field=>$label)<label class="text-sm font-semibold">{{ $label }}<input required type="{{ $field==='email'?'email':'text' }}" name="{{ $field }}" value="{{ old($field,$sendingAccount->$field) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label>@endforeach<label class="text-sm font-semibold">Daily limit<input required type="number" min="1" name="daily_limit" value="{{ old('daily_limit',$sendingAccount->daily_limit??50) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label><label class="text-sm font-semibold">Status<select name="status" class="mt-1 block w-full rounded-xl border-slate-200">@foreach(['paused','active','error'] as $value)<option value="{{ $value }}" @selected(old('status',$sendingAccount->status??'paused')===$value)>{{ ucfirst($value) }}</option>@endforeach</select></label></div></section>
@foreach(['smtp'=>'SMTP (sending)','imap'=>'IMAP (future inbox monitoring)'] as $prefix=>$title)<section class="rounded-2xl border bg-white p-6"><h2 class="font-bold">{{ $title }}</h2><div class="mt-4 grid gap-4 md:grid-cols-2"><label class="text-sm font-semibold">Host<input {{ $prefix==='smtp'?'required':'' }} name="{{ $prefix }}_host" value="{{ old($prefix.'_host',$sendingAccount->{$prefix.'_host'}) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label><label class="text-sm font-semibold">Port<input {{ $prefix==='smtp'?'required':'' }} type="number" min="1" max="65535" name="{{ $prefix }}_port" value="{{ old($prefix.'_port',$sendingAccount->{$prefix.'_port'}) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label><label class="text-sm font-semibold">Username<input {{ $prefix==='smtp'?'required':'' }} name="{{ $prefix }}_username" value="{{ old($prefix.'_username',$sendingAccount->{$prefix.'_username'}) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label><label class="text-sm font-semibold">Password<input {{ !$sendingAccount->exists&&$prefix==='smtp'?'required':'' }} type="password" name="{{ $prefix }}_password" autocomplete="new-password" placeholder="{{ $sendingAccount->exists?'Leave blank to preserve saved password':'' }}" class="mt-1 block w-full rounded-xl border-slate-200"></label><label class="text-sm font-semibold">Encryption<select name="{{ $prefix }}_encryption" class="mt-1 block w-full rounded-xl border-slate-200"><option value="">Select</option>@foreach(['tls','ssl','none'] as $value)<option value="{{ $value }}" @selected(old($prefix.'_encryption',$sendingAccount->{$prefix.'_encryption'}??($prefix==='smtp'?'tls':null))===$value)>{{ strtoupper($value) }}</option>@endforeach</select></label></div></section>@endforeach<div class="flex justify-end gap-3"><a href="{{ route('sending-accounts.index') }}" class="rounded-xl border px-5 py-2.5 text-sm font-bold">Cancel</a><button class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-bold text-white">Save securely</button></div></form></div></x-app-layout>
