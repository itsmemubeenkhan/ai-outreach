<x-app-layout><x-slot name="header"><div class="flex items-end justify-between"><div><h1 class="text-2xl font-bold text-slate-900">Leads</h1><p class="mt-1 text-sm text-slate-500">Server-side filtering built for large datasets.</p></div><a href="{{ route('leads.create') }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white">Add lead</a></div></x-slot>
<div class="p-5 lg:p-8"><form class="mb-5 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-4 xl:grid-cols-6">@foreach(['category','country','state','city','phone_type','source'] as $f)<select name="{{ $f }}" class="rounded-lg border-slate-200 text-sm"><option value="">{{ str($f)->replace('_',' ')->title() }}</option>@foreach($options[$f] as $v)<option @selected(request($f)===$v)>{{ $v }}</option>@endforeach</select>@endforeach
<select name="email_status" class="rounded-lg border-slate-200 text-sm"><option value="">Email status</option>@foreach(['unknown','valid','invalid','catch_all','bounced'] as $v)<option value="{{ $v }}" @selected(request('email_status')===$v)>{{ str($v)->replace('_',' ')->title() }}</option>@endforeach</select><select name="lead_status" class="rounded-lg border-slate-200 text-sm"><option value="">Lead status</option>@foreach(['new','queued','contacted','opened','clicked','replied','interested','not_interested','unsubscribed','closed'] as $v)<option value="{{ $v }}" @selected(request('lead_status')===$v)>{{ str($v)->replace('_',' ')->title() }}</option>@endforeach</select><select name="email_availability" class="rounded-lg border-slate-200 text-sm"><option value="">Any email</option><option value="yes" @selected(request('email_availability')==='yes')>Has email</option><option value="no" @selected(request('email_availability')==='no')>No email</option></select><select name="website_availability" class="rounded-lg border-slate-200 text-sm"><option value="">Any website</option><option value="yes" @selected(request('website_availability')==='yes')>Has website</option><option value="no" @selected(request('website_availability')==='no')>No website</option></select><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-bold text-white">Filter</button><a href="{{ route('leads.index') }}" class="py-2 text-center text-sm font-semibold text-slate-500">Clear</a></form>
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wider text-slate-500"><tr><th class="px-4 py-4"><input type="checkbox" aria-label="Select all leads"></th><th class="px-4 py-4">Name</th><th class="px-4 py-4">Business Name</th><th class="px-4 py-4">Email</th><th class="px-4 py-4">Phone</th><th class="px-4 py-4">Website</th><th class="px-4 py-4">Score</th><th class="px-4 py-4"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($leads as $lead)<tr class="hover:bg-slate-50"><td class="px-4 py-4"><input type="checkbox" value="{{ $lead->id }}" aria-label="Select {{ $lead->business_name ?: 'lead' }}"></td><td class="px-4 py-4 font-semibold text-slate-900">{{ $lead->contact_person ?: trim($lead->first_name.' '.$lead->last_name) ?: '—' }}</td><td class="px-4 py-4"><div class="font-semibold text-slate-900">{{ $lead->business_name ?: '—' }}</div><div class="text-xs text-slate-500">{{ $lead->category }}</div></td><td class="px-4 py-4">@if($lead->email)<a href="mailto:{{ $lead->email }}" class="text-indigo-600 hover:underline">{{ $lead->email }}</a>@else<span class="text-slate-400">No email</span>@endif</td><td class="whitespace-nowrap px-4 py-4">@if($lead->phone)<a href="tel:{{ $lead->phone }}" class="font-medium text-slate-700 hover:text-indigo-600">{{ $lead->phone }}</a>@else<span class="text-slate-400">No phone</span>@endif</td><td class="max-w-56 px-4 py-4">@if($lead->website)@php $websiteUrl = str_starts_with($lead->website, 'http://') || str_starts_with($lead->website, 'https://') ? $lead->website : 'https://'.$lead->website; @endphp<a href="{{ $websiteUrl }}" target="_blank" rel="noopener noreferrer" class="block truncate text-indigo-600 hover:underline">{{ $lead->website }}</a>@else<span class="text-slate-400">No website</span>@endif</td><td class="px-4 py-4"><span class="rounded-lg bg-slate-100 px-2.5 py-1 font-black text-slate-800">{{ $lead->lead_score }}</span></td><td class="px-4 py-4"><a href="{{ route('leads.edit',$lead) }}" class="font-bold text-indigo-600">Edit</a></td></tr>@empty<tr><td colspan="8" class="px-5 py-16 text-center text-slate-500">No leads match these filters.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 p-4">{{ $leads->links() }}</div></div></div></x-app-layout>
