<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold text-slate-900">{{ $lead->exists ? 'Edit lead' : 'Add lead' }}</h1></x-slot><div class="p-5 lg:p-8"><form method="POST" action="{{ $lead->exists ? route('leads.update',$lead) : route('leads.store') }}" class="mx-auto max-w-5xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">@csrf @if($lead->exists) @method('PUT') @endif
@if($errors->any())<div class="mb-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{{ $errors->first() }}</div>@endif
@php $fields=['business_name'=>'Business name','number_of_employees'=>'Employees','contact_person'=>'Contact person','first_name'=>'First name','last_name'=>'Last name','corporate_email'=>'Corporate email','email'=>'Email','website'=>'Website','phone'=>'Phone','phone_type'=>'Phone type','street_address'=>'Street address','zip_code'=>'ZIP code','state'=>'State','city'=>'City','country'=>'Country','category'=>'Category','source'=>'Source']; @endphp<div class="grid gap-5 md:grid-cols-2">@foreach($fields as $name=>$label)<label class="text-sm font-semibold text-slate-700">{{ $label }}<input type="{{ str_contains($name,'email') ? 'email' : ($name==='number_of_employees'?'number':'text') }}" name="{{ $name }}" value="{{ old($name,$lead->$name) }}" class="mt-1 block w-full rounded-xl border-slate-200 focus:border-indigo-500 focus:ring-indigo-500"></label>@endforeach
<label class="text-sm font-semibold text-slate-700">Email status<select name="email_status" class="mt-1 block w-full rounded-xl border-slate-200">@foreach(['unknown','valid','invalid','catch_all','bounced'] as $v)<option value="{{ $v }}" @selected(old('email_status',$lead->email_status ?: 'unknown')===$v)>{{ str($v)->replace('_',' ')->title() }}</option>@endforeach</select></label><label class="text-sm font-semibold text-slate-700">Lead status<select name="lead_status" class="mt-1 block w-full rounded-xl border-slate-200">@foreach(['new','queued','contacted','opened','clicked','replied','interested','not_interested','unsubscribed','closed'] as $v)<option value="{{ $v }}" @selected(old('lead_status',$lead->lead_status ?: 'new')===$v)>{{ str($v)->replace('_',' ')->title() }}</option>@endforeach</select></label><label class="text-sm font-semibold text-slate-700">Lead score<input type="number" name="lead_score" value="{{ old('lead_score',$lead->lead_score ?? 0) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label><label class="text-sm font-semibold text-slate-700">Next follow-up<input type="datetime-local" name="next_follow_up_at" value="{{ old('next_follow_up_at',$lead->next_follow_up_at?->format('Y-m-d\TH:i')) }}" class="mt-1 block w-full rounded-xl border-slate-200"></label></div><div class="mt-7 flex justify-end gap-3"><a href="{{ route('leads.index') }}" class="rounded-xl border px-5 py-2.5 text-sm font-bold">Cancel</a><button class="rounded-xl bg-indigo-600 px-5 py-2.5 text-sm font-bold text-white">Save lead</button></div></form></div></x-app-layout>
