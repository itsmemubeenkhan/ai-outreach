<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold">Inbox</h1><p class="text-sm text-slate-500">Replies are stopped before classification.</p></x-slot><div class="p-5 lg:p-8"><div class="mb-4 flex flex-wrap gap-2">@foreach([''=>'All','needs_action'=>'Needs Action','interested'=>'Interested','question'=>'Questions','pricing'=>'Pricing','callback'=>'Callback','later'=>'Later','out_of_office'=>'Out of Office','not_interested'=>'Not Interested','unsubscribe'=>'Unsubscribe','unmatched'=>'Unmatched'] as $key=>$label)<a href="{{ route('inbox.index',['filter'=>$key]) }}" class="rounded-full border bg-white px-3 py-2 text-xs font-bold">{{ $label }}</a>@endforeach</div><div class="overflow-x-auto rounded-2xl border bg-white"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="p-4">Contact</th><th>Business</th><th>Campaign</th><th>Subject / Reply</th><th>Received</th><th>Classification</th><th>Score</th></tr></thead><tbody class="divide-y">@forelse($messages as $m)<tr><td class="p-4"><a class="font-bold text-indigo-600" href="{{ route('inbox.show',$m) }}">{{ $m->from_name?:$m->from_email }}</a><div class="text-xs text-slate-500">{{ $m->from_email }}</div></td><td>{{ $m->lead?->business_name?:'Unmatched' }}</td><td>{{ $m->campaign?->name?:'—' }}</td><td class="max-w-sm"><div class="font-semibold">{{ $m->subject }}</div><div class="truncate text-slate-500">{{ $m->body_text }}</div></td><td>{{ $m->received_at->diffForHumans() }}</td><td><span class="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{{ $m->classification?:'pending' }}</span>@if($m->requires_human_action)<span class="ml-1 text-rose-600">●</span>@endif</td><td>{{ $m->lead?->lead_score??'—' }}</td></tr>@empty<tr><td colspan="7" class="p-16 text-center text-slate-500">No replies yet.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $messages->links() }}</div></div></div></x-app-layout>
