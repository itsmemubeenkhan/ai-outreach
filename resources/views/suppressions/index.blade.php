<x-app-layout><x-slot name="header"><h1 class="text-2xl font-bold text-slate-900">Suppression List</h1><p class="mt-1 text-sm text-slate-500">Every send checks this list again inside the delivery job.</p></x-slot><div class="p-5 lg:p-8"><form method="POST" action="{{ route('suppressions.store') }}" class="mb-5 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-4">@csrf<input required type="email" name="email" placeholder="email@example.com" class="rounded-xl border-slate-200"><select name="reason" class="rounded-xl border-slate-200">@foreach(['manual','unsubscribe','bounce','complaint','invalid'] as $value)<option value="{{ $value }}">{{ ucfirst($value) }}</option>@endforeach</select><input name="source" placeholder="Source (optional)" class="rounded-xl border-slate-200"><button class="rounded-xl bg-slate-900 px-4 text-sm font-bold text-white">Suppress email</button>@if($errors->any())<p class="text-sm text-red-600 md:col-span-4">{{ $errors->first() }}</p>@endif</form><div class="overflow-hidden rounded-2xl border bg-white"><table class="min-w-full divide-y text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="p-4">Email</th><th>Reason</th><th>Source</th><th>Added</th><th></th></tr></thead><tbody class="divide-y">@forelse($suppressions as $item)<tr><td class="p-4 font-semibold">{{ $item->email }}</td><td>{{ $item->reason }}</td><td>{{ $item->source?:'—' }}</td><td>{{ $item->created_at->format('M j, Y') }}</td><td><form method="POST" action="{{ route('suppressions.destroy',$item) }}">@csrf @method('DELETE')<button class="text-xs font-bold text-red-600">Remove</button></form></td></tr>@empty<tr><td colspan="5" class="p-16 text-center text-slate-500">The suppression list is empty.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $suppressions->links() }}</div></div></div></x-app-layout>
